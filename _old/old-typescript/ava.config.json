{
  "//": "ava is a test runner - see package.json",
  "typescript": {
    "//": "Both keys and values here must end with a /",
    "rewritePaths": {
      "src/": "build/",
      "test/": "build/test/"
    },
    "//compile": "compile before each run?",
    "compile": true
  }
}
